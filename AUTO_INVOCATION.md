# 自动调用边界

## 通用优先级

自动发现和调用依次服从：用户当前明确指令、当前阶段目标、当前仓库范围、仓库内 `AGENTS.md` 与项目规范、用户提供的设计参考、安全与合规边界、最小必要原则、Skill 自身默认说明。

Skill 不能扩大任务范围、覆盖用户决定或获得额外 Git、账号、发布和生产环境权限。

## agent-browser

可在 Web 页面或浏览器扩展现场验收、点击与只读检查、截图、响应式检查、本地 Web 应用验证、用户可见 Bug 回归及明确的 Electron 自动化测试中自动调用。

每次自动调用必须使用新的临时 session 和隔离 Profile，不读取或复用真实 Profile、Cookie、密码、浏览历史或登录状态；只访问任务必要页面；结束后关闭 session 并检查残留进程。临时截图不得含敏感信息，也不得进入备份仓库。

未经用户明确授权，禁止登录账号、输入账号凭据或验证码、绕过验证码或反自动化、导入 Cookie、访问隐私页面、操作 GitHub/邮箱/支付/商店后台、接受协议、购买、发布、发送消息、上传用户文件、修改或删除云端及生产数据。遇到登录、验证码、支付、发布、协议、敏感授权、真实 Profile 或个人数据要求时必须停止并报告。

自动调用不授予 Git push、修改其他仓库、账号操作、发布、绕权或读取真实浏览器数据的权限。

## frontend-design

可在新建页面或组件、调整现有 UI、Dashboard、桌面界面、浏览器扩展面板、设置页、卡片、导航、弹窗、表单、各类状态、字体、间距、层级、响应式、可访问性和视觉一致性工作中自动调用。

调用必须服从阶段验收、现有设计文档、截图或 Stitch 参考、品牌和产品约束；优先复用现有组件与 token；只修改当前 UI 目标直接相关的文件；不改变功能逻辑、数据边界和安全边界。

禁止借此重写架构、更换框架、引入大型设计系统或无必要依赖、修改无关页面、删除功能、改变信息架构或路线、擅增页面或功能、复制竞品或使用来源不明和未经授权的素材。纯后端、Git、数据迁移、文档收尾和普通逻辑修复不应强行触发 UI 改造。

## 联合调用

同时涉及 UI 实现与浏览器验收时，顺序为：`frontend-design` 指导实现，项目原有测试验证业务逻辑，lint/类型检查/构建验证质量，最后由 `agent-browser` 在隔离环境验证用户可见结果。联合调用仍受全部范围和安全边界约束。
